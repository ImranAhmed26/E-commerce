/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  distDir: "build",
};

module.exports = nextConfig;
module.exports = {
  images: {
    domains: ["idbdev.com"],
  },
};
